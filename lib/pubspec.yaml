name: ff_optimizer
description: "Free Fire Optimizer App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^9.1.2

flutter:
  uses-material-design: true
